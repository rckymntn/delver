import { Entity } from "./Entity";

/*
 *  A prop is an entity that is not an actor or tile. This is for things like decorations or items on the map
 */
export interface Prop extends Entity {

}

export const enum PropType {
    
}